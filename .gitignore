# Local settings — contains your password; never publish it.
config.py

# Python cruft
__pycache__/
*.pyc

# Per-PC settings (created by the Settings window)
settings.json

# Per-PC updater state (which commit is installed here)
.applied_commit

# Build output from BUILD.bat (never publish the big .exe to the repo)
build/
dist/
*.spec
